[package]
name = "avalanched-aws"
version = "0.0.9-pre"
edition = "2021"

[[bin]]
name = "avalanched-aws"
path = "src/main.rs"

[dependencies]
avalanche-sdk = { version = "0.0.4", features = ["cloudwatch"] }
avalanche-types = { version = "0.0.10", features = ["jsonrpc", "metrics"] }
avalanche-utils = { version = "0.0.3", features = ["full"] }
avalanchego = { path = "../avalanchego" }
avalancheup-aws = { path = "../avalancheup-aws" }
aws-sdk-cloudwatch = "0.13.0"
aws-sdk-manager = { version = "0.0.2" }
aws-sdk-s3 = "0.13.0"
clap = { version = "3.2.5", features = ["cargo", "derive"] } # https://github.com/clap-rs/clap/releases
env_logger = "0.9.0"
log = "0.4.17"
tempfile = "3.3.0"
tokio = { version = "1.18.2", features = ["full"] }
