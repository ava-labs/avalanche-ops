/// Downloads "avalanchego" from the remote storage (e.g., S3),
/// loads the "avalanche" configuration file (rather than flags),
/// sets up "avalanche" systemd service, and starts the service.
pub struct Todo {}
